# MEMORY.md — append-only 판단 로그

> 규칙: 절대 지우지 않는다. 판단이 바뀌면 아래에 새 항목을 append한다.

---

## 현재 진행 단계

- **Phase 0 완료 (2026-08-13)** — 문서 3종 + 스키마 작성. 반론 보고 후 승인 대기 중.
- **Phase 0 결정 반영 + Phase 2 착수 (2026-08-13)** — 반론 8건 전부에 사용자 결정 수신. CSV는 사용자가 준비 중이라 Phase 1은 대기, 외부 의존 없는 계산기(Phase 2)부터 구현.
- **Phase 2 빌드·실행·계산 검증 완료 (2026-08-13)** — Node v24.19.0 설치(사용자 승인) 후 `npm run build` 성공(타입 오류 0, 4개 라우트 정적 생성). dev 서버 실행 + 브라우저에서 홈/계산기/StructureEditor 흐름 실동작 확인. 샘플 3건(고/중/저위험) 계산: 수령 351만 → 확정 179 / 노출 137 / 세금유보 35, 런웨이 2.7개월 — 손계산과 일치. 구조값 덮어쓰기(선지급률 0.65→0.5, 1구간 환수율 100→80%) 시 노출 137→109, 배지 "내 조건 반영됨" 전환, structureSource=user 저장 확인. **알려진 한계**: 선지급률 변경은 기존 계약의 저장된 선지급액(advancePaid)에 소급 적용되지 않음 — 이미 받은 실금액이므로 의도된 동작이나, '추정치 버튼'으로 넣은 계약은 재추정되지 않는다는 점을 README 한계 항목에 쓸 것.
- (이전 기록) **Phase 2 코드 작성 완료, 빌드 검증 대기 (2026-08-13)** — 온보딩 5단계 + 계산기(4문항 위험 판정, 3분할, 런웨이) + 홈(접점 리스트 자리 + 수당 점검 배너) 구현. **이 PC에 Node.js가 미설치라 `npm install`/빌드를 못 돌렸다 — 코드는 미검증 상태이며 "동작 확인 완료"라고 주장하지 않는다.** Node 설치는 시스템 변경이라 사용자 허가 대기 (winget 사용 가능 확인됨).
- Phase 1 파이프라인 스크립트는 의도적으로 미작성 — 실제 CSV 컬럼명을 보기 전에 파서를 쓰는 것은 추측 코딩. CSV 도착 후 컬럼 확인 → 작성.

---

## 확정된 결정과 근거

- **[2026-08-13] 사실 검증 완료**: 1200%룰 GA 확대 2026-07-01 시행(금융위 보도), 13회차 정착률 생보 38.19%/손보 46.41%(금감원 공시 기반 보도). 웹 검색으로 교차 확인.
- **[2026-08-13] 정착률 추세 병기 결정**: 38.19%는 2023년 31.97%에서 개선된 수치. README에 절대 수준과 추세를 함께 쓴다. 개선 추세를 숨기면 평가자가 원자료 확인 시 신뢰 전체가 무너짐.
- **[2026-08-13] 계산기 포지셔닝**: "8월 소득이 왜 줄었나"의 원인 분해는 불가능(절판 교란) → "앞으로의 수령액 중 얼마가 미확정인가"를 보여주는 도구로 포지셔닝.
- **[2026-08-13] places.json에 경과개월·라벨을 굽지 않는다**: 정적 파일에 파생값을 넣으면 배포 다음 날부터 stale. 원본(인허가일자)만 저장하고 경과개월·단계라벨은 런타임 파생 계산. 스키마에서 Place(원본)와 PlaceView(파생)를 분리.
- **[2026-08-13] AgentProfile에 monthlyFixedExpense(월 고정지출) 추가**: 기획 원문의 온보딩 항목에 없으나 런웨이 = 현금 ÷ 월지출이므로 없으면 핵심 출력이 계산 불능. 사용자 승인 대기.

---

- **[2026-08-13] 반론 보고에 대한 사용자 결정 (전량 확정)**:
  - 월 고정지출 승인 — 단 한 칸("월세·대출·생활비 포함"), 기본값 = 3개월 수수료 평균의 70%, 슬라이더.
  - 환수 노출 계산식 확정 — Σ(선지급액 × 환수율 × 위험계수), 계수 상 0.6/중 0.3/하 0.1, 중위험 포함. **보수성 원칙**: 과소추정이 과대추정보다 해롭다. "생각보다 여유 있네"를 만들면 실패.
  - 위험 모델 수정 — 관계 단독 가중 → **조합 가중**. 위험한 것은 "내 부탁으로 든 지인 계약". 인터뷰 검증 문항으로 이관("환수 맞은 계약들 공통점").
  - **도구 가치 재정의** — 구조값 입력 요구 → 기본값 제안 + "내 조건 확인하는 법" 안내. 가치가 "계산"에서 "무엇을 모르는지 알려주기"로 확장 (PRD §0).
  - 익명 집계: 표본 수 상시 표시, 30건 미만 미표시.
  - **구현 순서 ≠ 화면 동선**: 구현은 계산기 먼저(외부 API 의존 없음), 홈 화면은 접점 리스트(매일 열 이유), 계산기는 "이번 달 수당 점검" 배너 진입.
  - 상품 5구분(생명/제3/손해일반/자동차/저축성), 소속사 4구분(전속생보/전속손보/GA대형/GA중소, 회사명 수집 금지), 세금 유보 기본 10%("예시값·세무 상담 권장" 필수).
  - 경과 구간: 갱신 도래 = 12±1개월(11~13), 14~23 = "1년차 경과". 이에 따라 관찰 구간을 7~10으로 조정(원 기획 7~11과 다름 — 갱신 창과 겹침 제거).
  - Phase 1 검증 확정: 인허가일-실개업일 표본 30건 대조, 10% 이상 어긋나면 경과 구간 재설계. 최초인허가일·영업상태변경일 필드 우선 확인.
- **[2026-08-13] "내 조건 확인하는 법"의 구체 항목(명세서 항목명, 위촉계약서 조항 위치)은 코드가 채우지 않기로 결정** — 회사별로 다른 실무 지식이며 추측 기재는 금지 사항. 구조 + 일반 문구만 구현, 구체 값은 사용자 TODO.

---

- **[2026-08-13] "내 조건 확인하는 법" 설계 변경 (사용자 결정)** — 명세서 항목명·계약서 조항을 도구가 알려주는 방식 **폐기**. 근거: 표기는 소속사마다 다르지만 본인 급여이므로 사용자는 자기 명세서를 읽을 수 있다. 새 방식: ① 기본값으로 즉시 결과 표시(입력 강제 금지) ② 결과 화면에 "내 실제 조건으로 다시 계산하기" 진입점 ③ 입력 칸 옆에는 항목명 단정 없이 개념 설명만("선지급과 관련된 항목", "환수·미경과로 표기된 항목") + 표기 상이 안내 ④ 기본값/사용자 입력을 `structureSource`로 구분 저장, 화면에 "기본값 사용 중 — 추정치" / "내 조건 반영됨" 배지 ⑤ 익명 집계는 사용자가 직접 입력한 구조값만 대상(기본값 재집계는 순환 — GIGO). 이로써 Phase 0의 미해결 항목 "확인법 구체 항목 사용자 작성 대기"도 소멸.

- **[2026-08-13] 화면 구조 개편 (사용자 결정)** — 계산 결과 자리에 입력 UI가 섞여 지저분하다는 지적. 동선을 랜딩(/) → /settings(A~F 섹션 통합, 온보딩 플로우 폐기) → /dashboard(결과 전용, 입력 요소 0개)로 재편. 계약 추가/수정은 /contracts로 분리. 구 라우트는 next.config redirects(/onboarding→/settings, /calculator→/dashboard)로 마이그레이션. 수령액 직접 입력(override) 필드는 "대시보드에 입력 폼 금지" 원칙에 따라 제거 — 수령액 = Σ 계약별 선지급액.
- **[2026-08-13] 온보딩의 '3개월 수수료 월별 3칸'을 '평균 1칸'으로 축소** (settings D 섹션 스펙) — AgentProfile.recentCommissions → avgCommission3m 타입 변경, 구 프로필은 storage.ts에서 평균 계산 마이그레이션.
- **[2026-08-13] 화면 개편 검증 완료** — 빌드 성공(라우트 5개, 타입 오류 0). 실동선 통과: 랜딩 → 시작하기 → /settings(GA대형, 평균 350, 현금 500, 고정지출 245 자동 제안) → 저장 → /dashboard(런웨이 2.0) → /contracts에서 고위험 계약 1건 추가(선지급 156 추정) → 대시보드 재확인(수령 156 / 확정 46 / 노출 94 / 세금 16, 런웨이 2.5 — 손계산 일치). **대시보드 입력 요소 0개 확인**(접근성 트리에 링크만 존재). /calculator→/dashboard, /onboarding→/settings 리다이렉트 동작 확인. 회사 최소치 미입력 시 충돌 경고 대신 안내 한 줄 표시 확인. 발견·수정: 고정지출 슬라이더 step=10에 제안값 245가 안 걸려 표시가 250으로 스냅되는 버그 → step=5로 수정.
- **[2026-08-13] 소속 구분별 구조 프리셋(B→C)은 4구분 모두 동일한 예시값으로 구현** — 구분별 실측 기준은 공개 데이터가 없고 추측 기재는 금지 사항. 익명 집계('user' 값만) 30건 이상 쌓이면 구분별 중앙값으로 대체하는 로드맵을 structure-presets.ts 주석과 PRD Phase 6에 연결.

- **[2026-08-13] 문구 제거 (사용자 지시)** — "소득 정보를 서버로 전송하지 않습니다" 및 동취지 문구를 전 화면에서 제거. 동작(로컬 저장, 금액 미전송, 옵트인 기본 OFF)은 유지, 설계 기록은 PRD·README·코드 주석에 존치. F 섹션 옵트인 문구는 "무엇이 집계되는지"만 남김(동의 정보로서 필요).
- **[2026-08-13] 지역 아키텍처 도입** — 지역 팩(public/data/regions/{시군구코드}/places·bjdong·meta.json) + 레지스트리(index.json 단일 출처) + 멱등 빌드 스크립트(build-region.ts). AgentProfile.region을 시군구코드로 변경('yongsan'→'11170' 마이그레이션). 자동 수집 워커는 미구현 결정(포털 로그인 다운로드 장벽) — README 로드맵에만 기록.
- **[2026-08-13] 행정구역 목록은 손으로 쓰지 않고 상가정보 CSV에서 추출** (extract-regions.ts, 277만 행 스캔). **데이터상 시도는 16개** — 광주+전남이 '전남광주통합특별시'(코드 12)로 통합돼 있고 화성시가 4개 구로 분구되는 등 2026년 개편이 반영돼 있다. 내 사전지식(17개 시도)과 달랐음 — 추출 방식이 아니었으면 지어낸 목록이 틀렸을 것. 강원 51, 전북 52 등 특별자치도 코드도 데이터 기준.
- **[2026-08-13] Phase 1 파이프라인 완료 (용산구 11170)** — 인허가 CSV는 CP949(안내문의 UTF-8 주장과 다름 → iconv 변환), 9개 업종 파일 836,048행 스캔 → 용산 24,375 → 영업중 7,192 → 좌표 결측 79 제외 → **7,113건**. 상가정보에서 법정동 36개 맵 추출. 인허가 CSV에 '최초인허가일' 필드는 없음(인허가일자뿐) — Phase 0 미해결 질문 해소.
- **[2026-08-13] 좌표계 확정: EPSG:5174** — 한국 bbox 검사는 5174/2097 둘 다 500/500이라 변별 불가. 인허가↔상가정보를 (동,번,지)+상호명으로 조인한 4,285쌍에서 상가정보 WGS84 좌표와의 중앙값 거리 비교: **5174 가정 2.1m vs 2097 가정 254.9m → 5174 확정** (실측 판정, 가정 아님).
- **[2026-08-13] 조인 실측 (용산)** — 인허가 지번 파싱 99.9%, (법정동,번,지) 키로 상가정보 조인 92.0% (6,542/7,113). 실패 8%는 주로 상가정보 커버리지 밖 업종(숙박업 등). 건축물대장 표제부/층별개요 CSV는 data/raw에 **없음** — 파일 확보 대기. 조인 키는 상가정보의 지번코드 19자리(법정동10+대지구분1+번4+지4)가 건축HUB API 파라미터와 동일 구조임을 확인.
- **[2026-08-13] 건축HUB 파라미터 파싱 유틸(src/lib/jibun.ts) 오프라인 검증** — 7,113건 중 7,109건(99.94%) 파싱 성공, 실패 4건은 지번주소 결측. 법정동코드는 bjdong.json(상가정보 원천) 매핑만 사용, 추측 금지. **API 테스트 호출은 .env.local에 인증키가 없어 보류** — 키 수령 대기.

- **[2026-08-13] 인허가일 표본 검증 (A5) 결과** — 3중 방법으로 수행:
  1. **외부 웹 검증 30건**: 판정가능 4건(일치 3: BHC이촌·레뚜왈·노드커피 / 불일치 1: 달볶이), 판정불가 26건. 미국 인덱스 검색으로는 네이버 리뷰·인스타 날짜가 노출되지 않고 네이버 도메인은 브라우징 정책 차단 — 이 방법 단독으로는 10% 기준 판정 불능. 지어내지 않고 그대로 기록.
  2. **내부 프록시(관리번호 연도 vs 인허가 연도)**: 최근 13개월 0%, 전체 0.4% — 단, 달볶이 사례처럼 신규 관리번호가 발급되는 재인허가는 잡지 못함(맹점 확인).
  3. **폐업↔신규 연결 실측(결정적)**: 달볶이의 전체 사슬이 원본에 있음 — 2001 인허가(일반음식점) → 2025-07-29 폐업 → 2025-08-14 같은 주소·상호로 휴게음식점 신규. 이를 일반화한 규칙(같은 (동,번,지)+유사 상호+폐업→인허가 간격 -30~180일)으로 최근 13개월 신규 699건 전수 측정: **재인허가 의심 39건, 5.6%**.
  - **판정: 10% 미만 → 경과 구간 재설계 불요.** 단 5.6%는 무시할 수준이 아니므로 다음 단계로 파이프라인에 '재인허가 의심' 플래그 추가를 제안(우선 접촉 리스트에서 표시/제외). 한계: 상호가 바뀐 양수도는 이 규칙으로 못 잡지만, 새 상호·새 사장은 설계사 접점 관점에서 신규 접점으로 유효하므로 위양성 방향이 아님.
- **[2026-08-13] 표본 검증 스크립트 3종 보존** — scripts/check-license-year.ts(프록시), scripts/check-relicense.ts(플래그 실측), scripts/analyze-join.ts(조인·좌표계). 재실행 가능한 증거로 유지.

- **[2026-08-13] 생존 분석 완료 — A4 가설 판정: 반증되지 않음, 단 재해석됨** (scripts/build-survival.ts → survival.json + /insights). 표본 24,315건(폐업 17,123 사건 + 영업 7,192 중도절단), 월 단위 Kaplan-Meier. 판정 기준은 분석 전 고정: "나이 m 사업장의 +24개월 조건부 생존율이 m=6에서 유의미하게 낮으면 반증". 결과: 나이 0~36개월 전 구간에서 82~83%(2015+ 코호트 77~80%)로 평탄 — **폐업 집중 구간이 접촉 직후에 오지 않는다. 경과 구간 상수 변경 불요.** 재해석 2가지: ① 어느 나이든 ~20%는 24개월 내 폐업 — 접점발 계약의 기저 환수 위험(계산기-접점 연결의 정량 근거). ② 업종 격차가 지배적(나이6 기준 휴게음식점 76.7% ~ 골프 98.4%) — **업종 필터는 위험 관리 장치**. 추가 발견: 0~2개월 조건부 폐업률(4.8%)이 3~6개월(2.4%)보다 높아 '관망' 라벨이 데이터로 지지됨.
- **[2026-08-13] 자동차 관련 데이터 조사하지 않기로 결정 (사용자 지시)** — 개인 차량 등록 정보는 공개되지 않으며(집계 통계만 존재) 시간을 쓰지 않는다.
- **[2026-08-13] 건축HUB API 테스트 호출 1건 성공** — getBrTitleInfo, 용산 후암동 103-10 (11170/10100/0/0103/0010): resultCode 00, 철근콘크리트구조·연면적 299.48㎡·지상4/지하1·제1종근린생활시설·사용승인 1993-11-04. **내진 필드 실재 확인**: rserthqkDsgnApplyYn(적용여부)·rserthqkAblty(내진능력) → BuildingInfo.earthquakeResistant 채울 수 있음(Phase 0 미해결 질문 해소). 전체 필드명 79개 확보. 인증키는 .env.local에만 존재(gitignore 확인) — 코드·저장소 미포함.

- **[2026-08-13] 생존 분석 판정 승인 (사용자)** — 경과 구간 상수 유지, 업종 위험 표시 방향 채택.
- **[2026-08-13] A. 표본 검증** — 업종별 조건부 생존율에 Greenwood 95% CI + 위험집합(atRisk) 산출. 규칙: **atRisk 30 미만은 점추정 미노출**("표본 부족" 표시, 접촉 권장 근거로 사용 금지). 용산 8개 업종은 전부 30 이상(최소 골프 63)이라 마스킹 대상 없음 — 단 골프 CI 95.3~100%로 넓어 CI 병기가 과신을 막음. /insights와 상세 패널 양쪽 적용.
- **[2026-08-13] B. 업종 위험 → 계산기 결합 (핵심)** — 결합식: `실효계수 = 1 − (1−품질계수)×(1−업종24개월폐업률×폐업시실효율)`. 독립 실패 경로의 합집합이라 결합계수 ≥ 단독 위험 (보수성 원칙). 폐업시 실효율 LAPSE_GIVEN_CLOSURE=0.7 **[미검증 가설]** 상수 분리. 업종 수치는 analyze 스크립트가 생성하는 industry-risk.generated.json이 단일 출처(하드코딩 금지, 표본부족 업종은 자동으로 평균 대체). **지시의 "미입력→평균"을 3상태로 세분화**: 업종 선택/사업장 아님(존속 0)/미입력(평균+추정 표시) — 사업장 없는 계약에 존속 위험을 잘못 씌우는 것 방지. 화면은 품질(계약자 요인)과 존속(구조 요인)을 분리 표시("품질 상 N · 존속 고위험 N"). 회귀 검증: 샘플 3건 노출 137→163만, 손계산 일치.
- **[2026-08-13] 2. 상품 구조 재정의** — needs-map을 업종→{business(사업 보장·진입 명분), personalContext(개인 보장 진입 맥락), employeeGroup(단체)}로 재구조화, 값은 전부 TODO(실무 지식, 추측 금지). 상세 패널에서 사업/개인 분리 표시. **상품 구분별 위험 가중치 검토 결과**: 화재(선지급 소액) vs 종신·연금(고액) 차이는 환수노출이 실제 선지급액(advancePaid)에 비례하므로 **이미 모델에 반영돼 있음** — 상품별 추가 가중은 근거 없어 도입 안 함. /insights에 "사업장 폐업→소득 단절→개인 보장 실효" 연결 섹션 추가.
- **[2026-08-13] 3. 접점 지도(/places) 구현** — 카카오맵 SDK(next/script, autoload=false, clusterer 라이브러리) + MarkerClusterer, 마커 4그룹 색상(우선 접촉 203/N주년 도래 1,586/업종 전환 의심 239/그 외 5,085), 필터(업종·동 36·그룹) 즉시 반영 실측. **카카오 키 부재 상태에서도 리스트·필터·상세 패널 동작**(동작하지 않는 화면 금지). 파이프라인에 재인허가 의심 플래그 통합(전수 239건, 직전 폐업일·이전 업종 근거 포함) 후 팩 재빌드. 상세 패널: 경과·주년, 업종 존속 요약(CI·표본 규칙), 전환 의심 근거, **건축HUB 온디맨드 조회 실동작 확인**(서버 라우트 /api/building, 서버·클라이언트 캐시, graceful degradation — 실측: 철근콘크리트·연면적 425.46㎡·내진 미적용), 니즈 "매핑 준비 중" 표시.
- **[2026-08-13] 4. 서울시 실시간 도시데이터 조사 (구현 안 함)** — 사이트 내부 API로 121개 장소 전수 확인. **용산구 관할 10곳**: 이태원역, 이태원 관광특구, 이태원 앤틱가구거리, 용산역, 용리단길, 삼각지역, 해방촌·경리단길, 국립중앙박물관·용산가족공원, 이촌한강공원, 노들섬 (+경계 걸침: 서울역, 남산공원). **연령대별 인구 비율 필드 실재 확인**(sample 키 실호출): LIVE_PPLTN_STTS.PPLTN_RATE_0/10/20/30/40/50/60/70, MALE/FEMALE_PPLTN_RATE, RESNT/NON_RESNT_PPLTN_RATE, AREA_CONGEST_LVL, FCST_PPLTN(예측). 구현 여부는 사용자 판단 대기.

- **[2026-08-13] 알림 채널: 카카오 대신 이메일 (사용자 결정, 근거 기록 지시)** — 카카오는 "나에게 보내기"만 검수 없이 가능, 타인 발송은 비즈앱 전환+검수 필요, 자동 메시지는 반려 사유 → 실사용자 전달 채널이 아님. Resend 이메일 채택. 어댑터 구조(send(payload, to, channel))로 email 구현, slack·kakao는 인터페이스만. 선정 로직은 순수 함수 pickTodayContacts로 분리 — 대시보드 "오늘 가볼 곳"과 메일 발송이 같은 함수 사용. Vercel Cron 매일 10시(KST=01 UTC), 옵트인(기본 OFF)만 발송, 메일에는 접점 정보만(금액 데이터 금지). **구독자 저장소는 로컬 파일 — Vercel 휘발성 FS 한계 README 명시** (금액 무저장 원칙 유지, 이메일만 명시 동의로 수집).
- **[2026-08-13] 가두 홍보(/outreach) 신설 — 접점 리스트와 분리** — 두 활동은 반대 방향(접점=사업주·한산할 때 / 홍보=행인·붐빌 때). 혼잡도·행사·연령 데이터는 홍보 쪽 지표로만 쓰고 접점 리스트에는 "혼잡 시간 방문 비권장" 한 줄만. 서울시 실시간 도시데이터 프록시(/api/citydata, 5분 캐시, 키 없으면 disabled·화면 생존). **EVENT_STTS 실필드 확인: EVENT_NM/PERIOD/PLACE/X/Y/PAY_YN/THUMBNAIL/URL/ETC_DETAIL — 지시에 있던 '카테고리' 필드는 존재하지 않음**(행사명·기간·장소·유료여부만 표시). 상주/비상주 해석·연령대 소구점(outreach-pitch.ts)은 [미검증 가설]/TODO 표기.
- **[2026-08-13] 전 화면 토스풍 통일** — 주 색상 #3182F6, 잉크 #191F28, 배경 #F9FAFB, 라운드 확대, Pretendard. 랜딩과 동일한 시스템으로 대시보드·설정·계약·지도·레이아웃 치환.

- **[2026-08-13] 계정 도입 (사용자 결정)** — 증상: 프로필 없으면 전 메뉴가 온보딩(/settings)으로 강제 이동. 해법: 이메일 회원가입(가입 시 메일 수신 동의 수집, 기본 OFF) → 온보딩 → 이후 자유 이동. 파일 기반 사용자 테이블(data/users.json, scrypt 해시 + HMAC httpOnly 세션 쿠키), /api/auth/*, /api/me. 설정은 언제든 수정 가능(보험사·자금 변동 대응)하고 서버에 저장된다.
- **[2026-08-13] ⚠️ 개인정보 원칙 판단 변경 (사용자 지시)** — Phase 0의 "금액은 로컬만" 원칙의 예외로, **프로필(보유현금·3개월 평균 포함)을 계정 테이블에 저장**한다. 근거: 크로스 기기 사용성. 계약별 금액은 여전히 브라우저 로컬 전용. 이 변경은 PRD §8.2와 상충하므로 README '판단이 바뀐 지점'에 기재할 것. 프로덕션 전 필수: SESSION_SECRET 설정, 파일 저장소→DB 교체(Vercel 휘발성 FS).
- **[2026-08-13] 지역별 데이터 중복 금지 (사용자 지시)** — 인사이트: 타 지역 폴백 제거, 지역 팩 없으면 "분석 없음" 표시("지역이 다르면 폐업 패턴도 다르다"). 홍보 포인트: 시군구→장소 매핑(OUTREACH_PLACES_BY_REGION, 용산만 조사 완료)으로 전환, 매핑 없는 지역은 빈 상태 — 서울시 API가 서울 121곳만 제공한다는 원천 한계 명시. 발송 대상은 사용자 테이블(수신 동의+지역 보유)로 일원화, 구 subscribers.json 제거.
- **[2026-08-13] 인증 흐름 실검증** — API: 가입→세션→프로필 PUT(보유현금 저장)→발송 dry-run이 사용자 테이블에서 1명·5건 선정. 브라우저: 게스트 가드, 가입 직후 온보딩 유도, 사용자 실계정(수신 동의 ON) 정상 동작 확인.

- **[2026-08-14] UI 피드백 5건 수정** — ① B→C 프리셋: 값이 전부 동일한 사실을 UI에 명시(지어내지 않는 결정 유지). ② 대시보드 히어로를 "환수·세금 대비로 N만원은 가지고 있어야" 중심으로 재구성(런웨이는 부제로; 계약 0건이면 런웨이 유지). ③ 추정치 배너를 "수수료 구조(선지급률·환수 구간)가 기본값"으로 명확화 — 자금 입력과 무관함을 표현. ④ 인사이트 존속 곡선 범례를 SVG 밖 HTML로 이동(곡선 겹침 해소). ⑤ 계절성 막대 미표시 버그 수정 — %높이가 auto 부모에서 0으로 붕괴 → px 높이 + 값 라벨. ⑥ 오늘 가볼 곳 업종 필터(pickTodayContacts에 categoryFilter 인자, 메일은 전체 유지).
- **[2026-08-14] LLM 통합 설계 접수 — 1단계(접근 시나리오) 구현** — 절대 원칙: 숫자는 코드, LLM은 해석·문장만; 비밀은 타입 수준 차단(types.ts에 금액·키 필드 부재) + 공통 출력 필터(guard.ts — 전 LLM 기능 공용, 기능별 분리 금지). 파인튜닝·멀티에이전트 기각(README 기록). /api/llm/scenario: 서버가 컨텍스트 결정론 구성(업종·경과·주년·전환 근거·업종 폐업률 실측), claude-opus-5 + 구조화 출력(json_schema), 로그인 필수 + 세션당 30회/일 리밋, refusal 처리. **법령 API 캐시 미보유 → 법령 필드를 컨텍스트에서 의도적으로 제외하고 출력에서 법령 언급 문장 제거** — 지어내지 않기. **ANTHROPIC_API_KEY 미설정이라 실호출 미검증 — 키 대기, disabled 상태로 가동.** 남은 단계: 2)롤플레잉+채점 3)음성 4)힌트 5)해석·구조화 6)지표 — 하나씩 검토하며 진행.

- **[2026-08-14] 국가법령정보 API 도입하지 않기로 확정 (사용자 결정)** — 법령 관련 기능 전체 제외. 롤플레잉 페르소나·채점 rubric에서 법정 의무 항목 제거. 단 출력 필터(guard.ts)의 법령 언급 차단은 유지 — LLM이 조문·과태료를 지어내는 것 방지 목적이므로 기능 제외와 무관하게 필요.
- **[2026-08-14] 롤플레잉은 음성 기본, 텍스트는 폴백 (사용자 결정)** — 모의면접 형태(중앙 상태 표시, 채팅 말풍선 나열 아님). 에코 방지: TTS 재생 중 STT 중지(기본), barge-in은 헤드폰 전제 옵트인. 인식 오류의 채점 오염 방지: confidence 임계 미달 발화는 채점 근거 제외 + 리뷰에서 전사 수정 → 재채점 + 실패율 높으면 채점 신뢰도 경고. **음성은 브라우저 구현체에 따라 외부 서버로 전송될 수 있음 — "금액은 로컬" 설계의 예외로 시작 전 고지·동의 필수** (README 기록).
- **[2026-08-14] 음성 롤플레잉 구현 완료 (서버 3라우트 + /roleplay 클라이언트)** — 서버: /api/llm/roleplay/turn(스트리밍, 문장 버퍼→guard→개행, 페르소나 매 턴 재주입, __DISABLED__/__RATELIMIT__/__ERROR__/__END__ 프로토콜), /score(rubric 코드 상수 고정, LLM은 기준별 판정+전사 인용, 코드가 인용 실재+저신뢰 제외 검증 후 가중합·힌트 감점 — 역할 분담 README 정직 기록), /hint(후보 2~3개, guard 통과분만). 클라이언트: setup(가상 설정 명시+다시 뽑기, 헤드폰 경고, barge-in 기본 OFF, 음성 개인정보 동의 없이는 시작 불가) → live(중앙 상태, interim 실시간, 마이크 레벨, 상단 사업장 고정, 힌트 카운트, 전사 접기, 텍스트 폴백 동일 기능) → review(기준별 판정+인용, 저신뢰 표시, 전사 수정→재채점). 진입은 접점 지도 상세 패널. **검증 범위 정직 고지: tsc 통과 + 브라우저에서 텍스트 폴백·disabled 폴백(셋업 복귀+키 대기 안내) 실동선 확인. 음성 경로(STT 재시작·무음 타이머·에코)는 마이크 권한이 필요해 자동 검증 불가 — 키·마이크 확보 후 수동 검증 필요.** 무음/신뢰도 임계 등 상수는 전부 [미검증 가설] 표기.

---

## 폐기된 접근과 폐기 이유

- (Phase 0 시점) PRD §5 참조: 신규 개업 알림 / 환수 확률 직접 질문 / 서버 소득 저장 / 건축물대장 전체 조인 / 자유 대화 챗봇 — 각 폐기 이유는 PRD에 기록.

---

## 미해결 질문

- ~~PRD §9의 7개 항목~~ → 2026-08-13 전량 확정 (위 결정 로그 참조). 남은 것: CSV 2종 도착 대기, "내 조건 확인하는 법" 구체 항목, needs-map 값
- 인허가일자가 양수도·재인허가 시 갱신되는지 → Phase 1에서 표본으로 확인
- 용산구 places.json 파일 크기 (수 MB면 초기 로드 문제) → Phase 1에서 측정
- 건축HUB API가 '내진 여부'를 직접 제공하는지 미확인 → Phase 4에서 응답 스펙 확인, 없으면 필드 optional 유지

---

## 사용자(이창호)에게 물어볼 것

- PRD §9 전체. 특히 **1번(월 고정지출)과 3번(환수 노출 계산식)은 Phase 2 착수 전 필수 답변**.
- needs-map 값은 Phase 3 착수 시점에 요청 예정 (보험 실무 지식 필요, 추측으로 채우지 않음).
